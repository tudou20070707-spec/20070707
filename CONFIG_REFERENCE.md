# STM32F103C8T6 + MPU9250 + OLED 姿态检测 — 配置参考

> 单文件版本，所有代码在 `user/main.c`，不依赖外部 MPU9250 驱动文件。

---

## 硬件连接

| STM32 引脚 | 连接 | 说明 |
|-----------|------|------|
| PB6 | MPU9250 SCL + OLED SCL | I2C1 时钟线 |
| PB7 | MPU9250 SDA + OLED SDA | I2C1 数据线 |
| PA0 | LED1 | 心跳灯 |
| PA1 | LED2 | 状态灯（常亮=正常） |
| PA2 | LED3 | 运动灯（倾斜>15°亮） |
| 3.3V | MPU9250 VCC + OLED VCC | 供电 |
| GND | MPU9250 GND + OLED GND | 共地 |

**上拉电阻：** 无外接，使用 STM32 内部上拉 (~40kΩ)。外接 4.7kΩ 后可提高 I2C 速率。

---

## I2C 通信

| 参数 | 当前值 | 说明 |
|------|--------|------|
| 速率 | **50 kHz** | 内部上拉极限值。外接 4.7kΩ 后可改为 100000 或 400000 |
| 超时 | 100000 次循环 | 每次 I2C 操作最多等待 ~5ms，超时后自动复位总线 |
| 引脚模式 | AF_OD + BSRR 上拉 | 复用开漏 + 内部上拉使能 |
| 直接寄存器轮询 | SR1.SB / ADDR / TXE / BTF / RXNE | 不用 `I2C_CheckEvent`，避免事件掩码兼容问题 |

### I2C 速率对照

| 速率 | 内部上拉 (~40kΩ) | 外接 4.7kΩ | 备注 |
|------|------------------|------------|------|
| 10 kHz | ✅ 稳定 | ✅ | 最保守 |
| **50 kHz** | ✅ 当前值 | ✅ | 推荐 |
| 100 kHz | ❌ 可能不稳 | ✅ | 标准模式 |
| 400 kHz | ❌ 不行 | ✅ | 快速模式 |

---

## 传感器配置

### MPU9250 6轴部分

| 寄存器 | 写入值 | 含义 |
|--------|--------|------|
| PWR_MGMT_1 | 0x00 | 唤醒，内部 8MHz 振荡器 |
| SMPLRT_DIV | 0x09 | 采样率 = 1kHz / (1+9) = **100 Hz** |
| CONFIG | **0x03** | DLPF = **44 Hz**（延迟 ~5ms） |
| GYRO_CONFIG | 0x00 | 陀螺仪 ±250°/s, 灵敏度 131 LSB/°/s |
| ACCEL_CONFIG | 0x00 | 加速度 ±2g, 灵敏度 16384 LSB/g |
| USER_CTRL | 0x00 | 关闭 I2C Master 模式 |
| INT_PIN_CFG | 0x02 | BYPASS_EN = 1，直通辅助 I2C 总线 |

### AK8963 磁力计部分

| 寄存器 | 写入值 | 含义 |
|--------|--------|------|
| CNTL1 | 0x00 → 0x16 | 先掉电再启动，**100Hz 连续测量**，16 位输出 |

- I2C 地址：扫描 0x0C、0x0D、0x0E（不同模块地址可能不同）
- 灵敏度：0.15 μT/LSB（16 位模式）
- 范围：±4800 μT

### DLPF 带宽对照

| DLPF_CFG | 带宽 | 延迟 | 适用场景 |
|----------|------|------|---------|
| 0x00 | 260 Hz | 0 ms | 高速运动 |
| 0x01 | 184 Hz | 2.0 ms | 快速响应 |
| 0x02 | 94 Hz | 3.0 ms | 一般用途 |
| **0x03** | **44 Hz** | **4.9 ms** | 姿态检测 ✅ |
| 0x04 | 21 Hz | 8.5 ms | 慢速运动 |
| 0x05 | 10 Hz | 13.8 ms | 振动环境 |
| 0x06 | 5 Hz | 19.0 ms | 强振动 ❌延迟太大 |

---

## 姿态解算流程

```
原始数据 (6轴 + 3轴磁力计)
  │
  ├─→ ① 陀螺仪零偏修正    g_corrected = g_raw - g_bias (开机采集400次求平均)
  │
  ├─→ ② 死区处理          |g| < DEADZONE → g = 0
  │    X/Y: 2 LSB          Z(Yaw): 10 LSB (磁力计修正后可以设大一些)
  │
  ├─→ ③ 低通滤波          指数移动平均
  │    陀螺仪: α=0.80     加速度: α=0.30     磁力计: α=0.20
  │
  ├─→ ④ Roll/Pitch 互补滤波
  │    运动时: 98% 陀螺仪 + 2% 加速度 → 跟手、抗振动
  │    静止时: 85% 陀螺仪 + 15% 加速度 → 加速收敛
  │
  └─→ ⑤ Yaw 陀螺仪+磁力计互补
        80% 陀螺仪积分 + 20% 磁力计航向 → 响应快且不漂移
        磁力计先做倾斜补偿 (用当前的 Roll/Pitch)
```

---

## 关键参数速查表

### 滤波参数 (`main.c` 顶部宏定义)

| 宏 | 值 | 含义 | 调大 | 调小 |
|----|-----|------|------|------|
| `GYRO_DEADZONE` | 2 | X/Y 陀螺仪死区 (LSB) | 抗抖更强 | 灵敏度更高 |
| `GYRO_DEADZONE_Y` | 10 | Z(Yaw) 陀螺仪死区 (LSB) | 抑制残余零偏 | Yaw 更灵敏 |
| `LPF_A_GYRO` | 0.80 | 陀螺仪低通 α | 响应更快/噪声更多 | 平滑/延迟大 |
| `LPF_A_ACCEL` | 0.30 | 加速度低通 α | 响应更快/振动敏感 | 平滑/延迟大 |
| `LPF_A_MAG` | 0.20 | 磁力计低通 α | 响应更快/干扰敏感 | 平滑 |
| `COMP_TRUST` | 0.98 | 运动中陀螺仪权重 | 更跟手/振动敏感 | 更平滑/延迟大 |
| `MAG_CORRECT` | 0.20 | 磁力计 Yaw 修正/周期 | 更快收敛到磁航向 | 抗干扰更强 |
| `DT` | 0.05 | 采样间隔 (秒) | — | — |
| `MAG_CORRECT` | 0.20 | 磁力计修正权重 | 收敛更快 | 抗磁干扰更强 |

### 收敛时间估算 (LPF)

| α | 时间常数 (周期) | @50Hz 时间常数 |
|----|-----------------|----------------|
| 0.80 | ~1.3 周期 | ~65 ms |
| 0.50 | ~2 周期 | ~100 ms |
| 0.30 | ~3.3 周期 | ~165 ms |
| 0.20 | ~5 周期 | ~250 ms |
| 0.15 | ~6.7 周期 | ~330 ms |
| 0.10 | ~10 周期 | ~500 ms |

---

## LED 状态指示

| LED | 位置 | 行为 | 含义 |
|-----|------|------|------|
| LED1 (PA0) | 心跳 | ~2Hz 闪烁 | 程序正常运行 |
| LED2 (PA1) | 状态 | 常亮 | MPU9250 通信正常 |
| LED2 (PA1) | 状态 | 灭 | WHO_AM_I 校验失败 |
| LED3 (PA2) | 运动 | 亮 | Roll 或 Pitch > 15° |

---

## OLED 显示

```
~~ 9-AXIS ATT ~~    ← 磁力计正常
~~ 6-AXIS ATT ~~    ← 磁力计未找到 (Yaw 漂移)

Roll : +012.3 deg
Pitch: -005.7 deg
Yaw  : +045.0 deg   ← 磁力计修正后不漂移
```

---

## 启动流程

```
1. LED_Init
2. I2C_Init_HW (50kHz + 内部上拉)
3. OLED_Init → 显示 "SYSTEM START"
4. Read WHO_AM_I → 显示值
5. MPU9250_Init_Simple
   - 写 PWR_MGMT_1 / SMPLRT_DIV / CONFIG / GYRO_CONFIG / ACCEL_CONFIG
   - 启用 BYPASS 模式
   - 扫描磁力计地址 (0x0C/0x0D/0x0E)
   - 找到 → 配置 AK8963 100Hz 连续模式
6. 显示 "MPU9250 FOUND!" + 磁力计状态
7. Calibrate (400次采样, 约2秒)
   - 陀螺仪零偏
   - 磁力计初始航向 (倾斜补偿)
8. 进入主循环 (50ms/周期)
```

---

## 常见问题 & 调优

| 现象 | 原因 | 解决方法 |
|------|------|---------|
| Yaw 一直漂 | 磁力计未找到 | 检查 `gMagOk`，标题显示 6-AXIS 则磁力计不通 |
| Yaw 一直漂 (9-AXIS) | MAG_CORRECT 太小 | 增大到 0.30~0.50 |
| 响应延迟大 | DLPF 或 LPF 太强 | 检查 DLPF ≥ 0x03, LPF_A_ACCEL ≥ 0.30 |
| 急停后数据跳动 | DLPF 太弱或 LPF 太快 | DLPF 降到 0x04, LPF_A_ACCEL 降到 0.20 |
| 振动时角度乱飘 | COMP_TRUST 太小 | 增大到 0.99 更信陀螺仪 |
| I2C 通信不稳定 | 速率太高 | 降到 20000 或 10000 |
| 启动卡住 | I2C 超时未恢复 | 检查接线和上拉，I2C_TMO 和 I2C_Recover 会自动复位 |

---

## 磁力计硬铁校准方法

当前版本磁力计偏移量设为 0。要正确校准（提高 Yaw 精度）：

1. 将模块水平放置，绕 Z 轴缓慢旋转 360°
2. 记录 MagX 和 MagY 的最大值和最小值
3. 计算偏移: `offsetX = (maxX + minX) / 2`, `offsetY = (maxY + minY) / 2`
4. 在 `Calibrate()` 函数末尾设置 `gMagOffX` 和 `gMagOffY`

---

## 添加外接上拉电阻后的调整

购买 4.7kΩ 电阻，接在 SCL/SDA 与 3.3V 之间后：

1. 删除 `GPIOB->BSRR = GPIO_Pin_6 | GPIO_Pin_7;` 这行（可选）
2. `I2C_ClockSpeed` 改为 100000 或 400000
3. `I2C_Recover` 内部可以去掉 `Delay_ms(1)`

---

> 最后更新：2026-07-27
> 文件位置：`user/main.c`
